---
title: "Listar campanhas"
api: "GET /user/campaigns"
description: "Lista todas as campanhas"
---

Este endpoint permite listar todas as campanhas.

### Campos da Resposta

<ResponseField name="campaigns" type="array">
  <Expandable title="propriedades">
    <ResponseField name="id" type="integer">
      ID da campanha
    </ResponseField>

    <ResponseField name="name" type="string">
      Nome da campanha
    </ResponseField>

    <ResponseField name="status" type="string">
      Status da campanha
    </ResponseField>

    <ResponseField name="max_calls_in_parallel" type="integer">
      Número máximo de chamadas que podem ser realizadas em paralelo
    </ResponseField>

    <ResponseField name="mark_complete_when_no_leads" type="boolean">
      Indica se a campanha deve ser marcada como concluída quando não houver leads
    </ResponseField>

    <ResponseField name="allowed_hours_start_time" type="string">
      Horário inicial permitido para realização de chamadas
    </ResponseField>

    <ResponseField name="allowed_hours_end_time" type="string">
      Horário final permitido para realização de chamadas
    </ResponseField>

    <ResponseField name="allowed_days" type="array">
      Dias da semana em que as chamadas são permitidas
    </ResponseField>

    <ResponseField name="max_retries" type="integer">
      Número máximo de tentativas de nova chamada para chamadas com falha
    </ResponseField>

    <ResponseField name="retry_interval" type="integer">
      Intervalo em segundos entre as tentativas de nova chamada
    </ResponseField>

    <ResponseField name="created_at" type="string">
      Data e hora de criação da campanha
    </ResponseField>

    <ResponseField name="updated_at" type="string">
      Data e hora da última atualização da campanha
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseExample>

```json 200 Response
[
  {
    "id": 1,
    "name": "Campanha de Teste",
    "status": "draft",
    "max_calls_in_parallel": 3,
    "mark_complete_when_no_leads": true,
    "allowed_hours_start_time": "00:00:00",
    "allowed_hours_end_time": "23:59:00",
    "allowed_days": [
      "monday",
      "tuesday",
      "wednesday",
      "thursday",
      "friday",
      "saturday",
      "sunday"
    ],
    "max_retries": 3,
    "retry_interval": 60,
    "created_at": "2025-05-29T07:18:34.000000Z",
    "updated_at": "2025-05-29T07:18:34.000000Z"
  }
]
</ResponseExample> ```
